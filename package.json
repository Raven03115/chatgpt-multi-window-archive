{
  "name": "chatgpt-multi-window",
  "version": "4.5.8",
  "private": true,
  "description": "Unofficial Windows multi-pane workspace for the official ChatGPT website",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.cjs",
    "diagnostics": "node scripts/show-diagnostics.cjs",
    "verify:base": "node scripts/verify-repository.cjs",
    "verify": "npm run verify:base && npm test"
  },
  "devDependencies": {
    "electron": "43.1.0"
  }
}
